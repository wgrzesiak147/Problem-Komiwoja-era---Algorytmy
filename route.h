#ifndef ROUTE_H
#define ROUTE_H

#include <QObject>
#include <QVector>

class Route : public QObject
{
    Q_OBJECT
public:
    //explicit Route(unsigned int size,QObject *parent = 0);
    explicit Route(unsigned int size,QObject *parent = 0);

signals:

public slots:

private:

};

#endif // ROUTE_H
